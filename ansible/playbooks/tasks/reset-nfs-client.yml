- hosts: all:!localhost
  gather_facts: false
  any_errors_fatal: true
  vars:
    vars_path_arg: "--vars-path {{ ki_tmp_vars_path }}"
  tasks:
    - when: target_node is none or inventory_hostname == target_node
      block:
        - name: Execute reset-nfs-client.sh
          shell: "sudo {{ ki_opt_scripts_path }}/all-node/nfs-client/reset-nfs-client.sh {{ vars_path_arg }}"
