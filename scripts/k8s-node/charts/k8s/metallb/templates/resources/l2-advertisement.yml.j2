apiVersion: metallb.io/v1beta1
kind: L2Advertisement
metadata:
  name: {{ name }}
  namespace: metallb
spec:
  ipAddressPools:
    - {{ pool_name }}
  nodeSelectors:
{%- for knn in knn_list %}
    - matchLabels:
        kubernetes.io/hostname: {{ knn }}
{%- endfor %}
